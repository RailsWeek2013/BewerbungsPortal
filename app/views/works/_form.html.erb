<%= form_for(@work) do |f| %>
  <% if @work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work.errors.count, "error") %> prohibited this work from being saved:</h2>

      <ul>
      <% @work.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time_start %><br>
    <%= f.date_select :time_start , :start_year => 1900, :end_year => 2100 %>
  </div>
  <div class="field">
    <%= f.label :time_end %><br>
    <%= f.date_select :time_end , :start_year => 1900, :end_year => 2100 %>
  </div>
  <div class="field">
    <%= f.label :Description%><br>
    <%= f.text_area :desc , :cols => 70, :rows => 20  %>
  </div>
  <div class="field">
    <%= f.hidden_field :type, :value => "Work" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>